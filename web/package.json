{
  "name": "web",
  "scripts": {
    "build": "elm-watch make && vite build",
    "codegen": "elm-gql init http://localhost:4000/graphql --force",
    "dev:codegen": "chokidar \"../backend/schema/*\" \"./src/**/*.gql\" -c \"sleep 1 && pnpm codegen\"",
    "dev:server": "vite",
    "dev:elm": "elm-watch hot"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.13",
    "chokidar-cli": "^3.0.0",
    "elm-gql": "^0.5.0",
    "elm-review": "^2.9.1",
    "tailwindcss": "^3.2.7",
    "vite": "^4.1.1"
  }
}
